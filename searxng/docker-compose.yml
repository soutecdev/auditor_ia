version: '3'
services:
  searxng:
    image: searxng/searxng:latest
    container_name: sonia-searxng
    restart: always
    ports:
      - "8888:8080"
    volumes:
      - ./settings.yml:/etc/searxng/settings.yml
    environment:
      - SEARXNG_BASE_URL=http://localhost:8888
